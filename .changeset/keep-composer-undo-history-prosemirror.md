---
default: minor
---

Keep the composer's undo (Ctrl+Z) history after sending, so a message you just sent can be restored back into the chatbox, and repeated undo walks back through everything you previously typed in the room, skipping the empty checkpoints each send leaves.
